{
  "name": "undercut-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "cd web && npm install && npm run build && rm -rf ../dist && cp -r dist ../dist",
    "dev": "cd web && npm run dev"
  }
}
