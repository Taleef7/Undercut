# Web frontend

This directory contains the project's web frontend.

## Stack

- React
- TypeScript
- Vite

## Development notes

This README intentionally documents the frontend at a high level instead of repeating the stock Vite template text. For implementation details, refer to the source in this directory and the local configuration files such as:

- `package.json`
- `vite.config.*`
- `tsconfig*.json`
- `eslint.config.*`

## Maintenance

When the frontend's purpose, scripts, or architecture change, update this README so it continues to describe the actual application rather than the default framework template.
